let variables = [firstName, lastName, sureName, userName, age, issolver, doesHatesToCode, sampleArray, person]

variables.forEach(variable => {
    console.log(typeof variable)
});

console.log(age,urage);